
<!-- Module configuration -->

<configuration>

  <section name="Module">

    <section name="Refresh">

      <!--
        Refresh auctions automatic every x minutes, 0 disables automatic refresh
      -->
      <config name="MaxAge" type="int">0</config>

      <!--
        Full refresh of auctions, all informations
      -->
      <config name="FullRefresh" type="bool">FALSE</config>

      <!-- *******************************************************************
      **  Don't change from here
      ******************************************************************** -->

      <config name="LoginRequired" type="bool">TRUE</config>

      <config name="Name">Refresh auctions, also automatic each x minutes</config>

      <config name="Author">Knut Kohl</config>
      <config name="Email">knutkohl@users.sourceforge.net</config>

      <config name="Version">0.4.0</config>

    </section>

  </section>

</configuration>
