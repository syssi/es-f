<configuration>

  <section name="Plugin">

    <section name="NextAuction">

      <!--
        Output style
        0 : Display as anchor line above auction table
        1 : Display via frontend status messages
      -->
      <config name="Style" type="int">0</config>

      <!--
        Show from no. of auctions and above, 0 or 1 means always
      -->
      <config name="Count" type="int">0</config>

      <!-- *******************************************************************
      **  Don't change from here
      ******************************************************************** -->

      <config name="Name">Show a link to the next ending auction on top position</config>

      <config name="Category">Interface</config>

      <config name="Author">Knut Kohl</config>
      <config name="Email">knutkohl@users.sourceforge.net</config>

      <config name="Version">0.2.0</config>
    </section>

  </section>

</configuration>
